export enum FileManagerApiUrl {
  drive = 'drive',
  download = 'download',
  // below is the new Url
  drives = 'Drives',
  storage = 'Storage',
  folders = 'Folders',
  files = 'Files',
  rename = 'Rename',
}
