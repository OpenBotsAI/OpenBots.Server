import { AllFileSizePipe } from './all-file-size.pipe';

describe('AllFileSizePipe', () => {
  it('create an instance', () => {
    const pipe = new AllFileSizePipe();
    expect(pipe).toBeTruthy();
  });
});
